# Train_Reservation_System
Train Reservation System uses an array to track seat availability, a linked list to store confirmed passengers, and a queue for the waiting list. If seats are available, a passenger gets a seat; if not, they are added to the waiting queue.
